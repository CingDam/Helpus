<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="detail">
	<select id="search" resultType="Detail" >
		SELECT detail.* FROM detail JOIN keyword ON detail.key_code = keyword.key_code
		WHERE keyword.key_name LIKE CONCAT(CONCAT('%',#{keyword}),'%')
	</select>
</mapper>