<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co_inqury">
	<insert id="add">
		INSERT INTO co_inqury (co_code,user_code,inqury_reg_date)
		SELECT #{coCode},#{userCode},NOW() FROM DUAL
		WHERE NOT EXISTS (
			SELECT co_code,user_code,inqury_reg_date FROM co_inqury
			WHERE co_code=#{coCode} AND user_code=#{userCode} AND inqury_reg_date=DATE_FORMAT(NOW(), '%Y-%m-%d')
		)
	</insert>
</mapper>